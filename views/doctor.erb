<h3><%= @doctor.name() %></h3>
<h5>Specialty: <%= @doctor.specialty() %></h5>

<% if @doctor.patients().any?() %>
  <h3>Here are all of the patients for this doctor:</h3>

  <ul>
    <% @doctor.patients().each() do |patient| %>
      <li><%= patient.name() %> </li>
    <% end %>
  </ul>

  <% else %>
    <p>There are no patients assigned to this doctor yet.</p>
  <% end %>


  <div class="container">
    <a href="/">Home</a>
    <a href="/doctors">View All Doctors</a>
  </div>
